# 12 Custom Token Kalman Signal

Demonstrates an external custom token surface. Validation does not execute the Python entrypoint;
execution remains controlled by the custom runtime approval/grant boundary.
