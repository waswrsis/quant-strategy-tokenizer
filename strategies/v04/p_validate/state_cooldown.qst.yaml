schema_version: qst-ir-schema/0.4
ir_version: qst-ir/0.4
canonical_version: qst-canonical/0.4
capabilities:
  - core
strategy:
  id: state_cooldown
  version: 1
  nodes:
    - id: cooldown_fsm
      token_ref:
        namespace: core
        name: state.fsm
        version: 1
        behavior_version: 1
      params:
        recipe: cooldown
      signature:
        inputs:
          event:
            type: EventStream[string]
        outputs:
          state:
            type: State[string]
  outputs:
    state: cooldown_fsm.state
metadata:
  p_validate: PV-A
  p_validate_case: state_cooldown
  fixture: fixtures/v04/p_validate/state/state_cooldown.json
