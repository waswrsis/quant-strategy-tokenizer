schema_version: qst-ir-schema/0.4
ir_version: qst-ir/0.4
canonical_version: qst-canonical/0.4
capabilities:
  - core
strategy:
  id: state_market_freeze
  version: 1
  nodes:
    - id: market_freeze_fsm
      token_ref:
        namespace: core
        name: state.fsm
        version: 1
        behavior_version: 1
      params:
        recipe: market_freeze
      signature:
        inputs:
          event:
            type: EventStream[string]
        outputs:
          state:
            type: State[string]
  outputs:
    state: market_freeze_fsm.state
metadata:
  p_validate: PV-A
  p_validate_case: state_market_freeze
  fixture: fixtures/v04/p_validate/state/state_market_freeze.json
