# Agent Methodology

1. Read the affected model and tests before editing.
2. Preserve canonical, hash, and security boundaries unless the task explicitly changes them through an ADR.
3. Use deterministic local commands for evidence.
4. Keep generated or temporary files out of the committed tree.
5. Prefer current `qst` imports and `.gkr.yaml` examples.