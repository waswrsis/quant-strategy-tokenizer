# Strategy Intent Schema

prompt_system_version: qst-stage-3c-v0.3.2.1
schema_type: prompt_schema

Required fields:

- strategy name
- target profile
- data inputs
- signal intent
- decision intent
- state requirements
- risk requirements
- weighting requirements
- external requirements
- custom logic requirements

Classification must use: `supported`, `partially_supported`, `reserved`, `custom_token_required`, `non_goal`.
