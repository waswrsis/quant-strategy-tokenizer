{
  "recipe": "gate.cooldown",
  "version": 1,
  "params_schema": {
    "field": {"type": "string", "default": "cooldown_elapsed"},
    "threshold": {"type": "number", "default": 0},
    "default": {"type": "number", "default": 0}
  },
  "inputs": {
    "state": "State"
  },
  "outputs": {
    "value": "elapsed.value"
  },
  "graph": [
    {
      "id": "elapsed",
      "recipe": "gate.elapsed_threshold",
      "v": 1,
      "params": {
        "field": "$params.field",
        "threshold": "$params.threshold",
        "default": "$params.default"
      },
      "inputs": {
        "state": "$inputs.state"
      }
    }
  ],
  "description": "Alias recipe for elapsed-threshold cooldown gates."
}
